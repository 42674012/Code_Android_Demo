<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/toolbar_start</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="loading_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 设置背景色 透明-->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 设置是否显示背景 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 设置背景透明度 -->
        <item name="android:backgroundDimAmount">0</item>
        <!-- 设置点击空白不消失 -->
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

    <style name="tab_image">
        <!-- Customize your theme here. -->
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

    <style name="tab_text">
        <!-- Customize your theme here. -->
        <item name="android:textSize">11sp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:textColor">@color/color_tabbar_text</item>
    </style>

    <style name="ontextoff" parent="Theme.AppCompat.Light">
        <item name="android:textColor">#0d09f1</item>
        <item name="android:textSize">8sp</item>
    </style>

    <style name="scstyle" parent="Theme.AppCompat.Light">
        <!--开启时的颜色-->
        <item name="colorControlActivated">#00e6db</item>
        <!--关闭时的颜色-->
        <item name="colorSwitchThumbNormal">#e0e0e0</item>
        <!--关闭时的轨迹颜色-->
        <item name="android:colorForeground">#ecd502</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <!-- 时间选择器-->
    <style name="dialog_normal" parent="@android:style/Theme.Dialog">
        <!-- 设置dialog区域（包含标题部分）前景，会遮盖windowContentOverlay  windowBackground   BackgroundDimEnabled   -->
        <item name="android:windowFrame">@null</item>
        <!-- 表示浮在屏幕上： true  在屏幕中心，相当于浮在页面上；  false 相当于整个盖在activity上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否全屏，配合android:windowIsFloating = false使用  全屏时会遮盖状态栏 -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <!-- 设置dialog是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <style name="Theme.Night" parent="Theme.AppCompat.DayNight">
        <item name="actionSheetStyle">@style/ActionSheetStyleiOS7</item>
        <item name="android:windowBackground">@color/default_window_bg_night</item>
        <item name="android:listSelector">?clickableBackground</item>
        <item name="clickableBackground">@drawable/clickable_background_night</item>
        <item name="font_color">@color/ssxinheihui10_press</item>
        <item name="main_bg">@color/ss_menu_text</item>
        <item name="divider_black">@color/default_window_bg_night</item>
        <item name="font_color2">@color/ssxinheihui10_press</item>
        <item name="divider_light_grey">@color/ssxinheihui10_press</item>
        <item name="font_red">#D12308</item>
        <item name="white_bg">@color/subscribe_category_bar_bg_night</item>
        <item name="windowNoTitle">?android:windowNoTitle</item>
        <item name="tab_bg">@color/colorPrimary</item>
    </style>
</resources>
